# msh  

**msh** (Mini Shell) is a custom implementation of a Unix shell, inspired by Bash. It is developed as part of the 42 School curriculum, providing a deeper understanding of process management, system calls, and shell behavior.

## 🚀 Features  
- Execute most Unix commands  
- Handle multiple built-in commands  
- Support for input/output redirection (`>`, `<`, `>>`)  
- Implement pipes (`|`) for command chaining  
- Environment variable management (`export`, `unset`, etc.)  
- Handle signals (e.g., `Ctrl+C`, `Ctrl+D`, `Ctrl+\`)  
- Basic command history  

## 🛠 Installation  
Clone the repository:  
```bash
git clone https://github.com/aalahyan3/msh.git
cd msh
make
